import { Typography } from '@mui/material';
import React from 'react';

function Logo() {
  return (
    <Typography
      variant="h6"
      sx={{
        fontWeight: 'bold',
      }}
    >
      LOGO
    </Typography>
  );
}

export default Logo;
